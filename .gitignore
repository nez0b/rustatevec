# Rust
/target
**/target
# Cargo.lock IS committed (this workspace ships binaries: qsv-cli, qsv-bench)

# Reference simulators (large; cloned locally for reference, not versioned here)
/references/
/_local/

# mdBook build output (regenerated by `mdbook build docs`)
/docs/book/

# Benchmark artifacts / criterion reports
/target/criterion/
/bench/golden/*.npy
/bench/results/*.csv

# OS / editor
**/.DS_Store
*.swp
.idea/
.vscode/

# Python (bench harness venv)
.venv/
__pycache__/
*.pyc
